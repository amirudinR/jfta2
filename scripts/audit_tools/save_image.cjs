const fs = require('fs'); const path = require('path'); const [tempPath, no, kanji] = process.argv.slice(2); const targetName = String(no).padStart(3, '0') + '_' + kanji + '.jpg'; const destPath = path.join('public/nemonik/kanji_nama_opt', targetName); fs.copyFileSync(tempPath, destPath); const data = JSON.parse(fs.readFileSync('public/nemonik/data.json')); const item = data.find(d => d.no == no); item.img_kanji_nama = 'kanji_nama_opt/' + targetName; fs.writeFileSync('public/nemonik/data.json', JSON.stringify(data, null, 2)); console.log('Saved ' + targetName);
